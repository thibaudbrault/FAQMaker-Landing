export interface FeatureItem {
  description: string;
  icon: string;
  title: string;
}

export interface FooterLink {
  text: string;
  url: string;
}

export interface NavItem {
  title: string;
  url: string;
}
